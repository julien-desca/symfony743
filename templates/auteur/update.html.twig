{% extends 'base.html.twig' %}

{% block body %}
  {{ form_start(formulaire)}}
    {{form_row(formulaire.nom)}}
    {{form_row(formulaire.prenom)}}
    {{form_row(formulaire.sauvegarder)}}
  {{ form_end(formulaire)}}
{% endblock %}
